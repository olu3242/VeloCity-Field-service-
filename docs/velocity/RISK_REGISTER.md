# Risk Register (Platform Certification Batch, Phase 9)

| # | Risk | Likelihood | Impact | Severity | Mitigation | Owner doc |
|---|---|---|---|---|---|---|
| 1 | `agent_logs` lacks RLS — relies entirely on app-level admin-only access patterns | Low (no current code path exposes it to non-admins) | Medium (if a future feature queries it with a user-scoped client) | Medium | Add `ENABLE ROW LEVEL SECURITY` + admin-only policy before any new feature reads this table from a non-admin context | `SECURITY_MULTITENANCY_CERTIFICATION.md` |
| 2 | Linear (not exponential) automation retry backoff | Medium (will matter once event volume grows) | Low-Medium (delays recovery, doesn't lose data) | Low-Medium | Switch `worker.ts` backoff formula to exponential before scaling event volume materially | `AUTOMATION_FABRIC_CERTIFICATION.md` |
| 3 | No automated alerting on automation failure spikes | Medium | Medium (admin must notice manually via dashboard) | Medium | Add a threshold-based alert (email/Slack) when `failed` count crosses a limit, before Franchise OS introduces more event volume from new franchisee operators | `AUTOMATION_FABRIC_CERTIFICATION.md` |
| 4 | Expansion/Commercial agent extension methods have no autonomous trigger | High (they will simply never run unless someone visits Command Center) | Low (no business flow currently depends on their output firing autonomously) | Low | Decide whether these need a cron/event trigger before they're relied upon operationally; do not assume they run in the background today | `AGENT_WORKFORCE_CERTIFICATION.md` |
| 5 | No live-environment validation exists for any certification in `docs/velocity/` (this batch or prior) | Certain (no live Supabase/Stripe/Vercel in this sandbox) | High if a code-path assumption is wrong and only surfaces in production | High until a live staging pass is run | Run a live staging pass (real Supabase project, real Stripe test mode, real Vercel cron) covering all three certified E2E journeys before launch | `E2E_JOURNEY_CERTIFICATION.md`, `PERFORMANCE_BASELINE.md` |
| 6 | `membership_entitlements`/`provider_certification_requirements` RLS policies are globally readable (`using (true)`) | Low | Low (catalog-style data, not currently sensitive) | Low | Scope to tenant if these become tenant-customizable in a future batch (e.g., Franchise OS) | `SECURITY_MULTITENANCY_CERTIFICATION.md` |
| 7 | No dead-letter table / circuit breaker for systemically failing automation handlers | Low at current scale | Medium (a misbehaving third-party integration could exhaust retries across many queue items independently) | Low-Medium | Add a circuit-breaker check before Franchise OS introduces new external integrations (e.g., per-franchise payment processors) | `AUTOMATION_FABRIC_CERTIFICATION.md` |
| 8 | Command Center page load performs many uncached parallel queries, growing with each batch's read-time intelligence layer | Medium (grows monotonically with each new batch) | Low today, Medium at higher admin-traffic scale | Low-Medium | Consider a caching layer (e.g., short-TTL server cache) for Command Center reads before adding more read-time intelligence sections in future batches | `PERFORMANCE_BASELINE.md` |

## Top risk before scope expansion

**Risk #5** is the dominant risk: every certification produced across this entire engagement (this batch and the two prior expansion/membership batches) is a static code-path certification, not a live-traffic validation. Before beginning Franchise OS / Enterprise Expansion / National Marketplace / Internationalization, a live staging pass exercising the three certified journeys end-to-end is the highest-value next step — higher value than any of the smaller hardening items above.

**Status: REGISTERED ✅** — no risk in this register is rated severity-critical; none blocks this batch's acceptance gate, but Risk #5 should be resolved before further scope expansion per the user's explicit instruction.
