# Evidence Completeness Certification (Remediation & Go-Live Hardening Batch, Phase 5)

Re-trace of every agent's Trigger → Execution → Evidence → Visibility chain by following actual call sites (`router.ts` switch cases, handler files, API routes), not by reusing the prior certification's prose uncritically — this batch's Phase 3 work showed that prose can drift from the real call graph. Where this re-trace confirms the prior `AGENT_WORKFORCE_CERTIFICATION.md` finding, it is noted as confirmed; where it differs, the difference is called out explicitly.

| Agent | Trigger (verified) | Execution | Evidence | Visibility | Re-trace result |
|---|---|---|---|---|---|
| **ALICE** | `router.ts:62` `case "service_request_created"` → `handleAliceIntake` | Synchronous within the queued handler | `agent_logs` row per `BaseAgent.log()`; writes `jobs.ai_classification`/`status` | Command Center agent activity table | Confirmed |
| **MAX** | `router.ts` `case "serviceability_passed"` → `handleMaxDispatch` | Synchronous within handler; **now also calls `assessCommercialDispatchPriority` before `MAX.match()`** (this batch's fix) | `agent_logs` for the LLM match call; `provider_offers`/`jobs.status` writes for the dispatch outcome | Command Center agent activity table | **Changed by this batch** — commercial narrowing is now reachable, not dormant |
| **QUINN** | (a) `POST /api/quotes` synchronous; (b) `router.ts:108` `case "quote_submitted"` → `handleQuinnQuote` | Dual path, both reachable | `agent_logs`; writes `quotes.metadata.quinn_analysis` | Command Center agent activity table | Confirmed |
| **NOVA** | (a) `analyzeTransition`/`scheduleReminders` via job-state events; **(b) `assessMarketDemand`/`assessMarketSupply`/`recommendExpansionOpportunities` now called from `handleTessTerritory` on the `daily_territory_analysis` cron** (this batch's fix) | (a) Queued via `handleNovaWorkflow`; (b) inline in TESS's existing daily handler | `agent_logs` for (a); `market_demand`/`market_supply`/`market_metrics`/`market_opportunities` table writes for (b) — these are evidence rows in their own right, written as a side effect of the compute functions, independent of `agent_logs` | Command Center agent activity table for (a); Command Center's expansion/territory section reads the same `market_*` tables for (b) | **Changed by this batch** — (b) was previously unreachable from any code path; now runs daily |
| **REX** | (a) `POST /api/reviews` synchronous; (b) `router.ts` `case "job_completed"`/`"customer_confirmed"`/`"provider_scoring"` → `handleRexCompletion` | Dual path; `provider_scoring` uses a deterministic non-LLM branch | `agent_logs` for LLM-backed paths only; `providers.trust_score` updated on every path including the deterministic one | Command Center agent activity table + avg-trust-score card | Confirmed, including the known under-reporting of execution count for the deterministic path (see `KNOWN_LIMITATIONS.md`) — this is accepted, not a defect, since the trust-score write itself is real evidence even without an `agent_logs` row |
| **IVY** | (a) `POST /api/disputes` synchronous; (b) `router.ts:155` `case "dispute_opened"`/`"dispute_resolved"` → `handleIvyDispute` | Dual path | `agent_logs`; `disputes.ai_recommendation`/`metadata.ivy_analysis`; payout freeze/unfreeze via `payout_queue` | Command Center agent activity table | Confirmed |
| **FINN** | `router.ts` `payment_*`/`payout_*`/`quote_approved` family → `handleFinnPayment`/`handlePayoutRelease` | Queued | `agent_logs` for LLM-backed methods; `revenue_records`/`payments`/`payout_queue` writes for deterministic financial logic | Command Center agent activity table + Commercial Revenue card (reading `computeCommercialRevenueIntelligence()` directly, not through FINN) | **Changed by this batch** — `calculateCommercialRevenue()` removed as dead code (see `AGENT_TRIGGER_CERTIFICATION.md`); FINN's evidenced surface is now exactly its LLM-backed payout/reconciliation methods plus `estimateJobEconomics()` (a synchronous pricing pass-through, unaffected) |
| **LENA** | Queued events (`review_requested`/`subscription_due`/etc.) + daily 3 AM cron emitting `retention_campaign` → `router.ts:167` | Queued via `handleLenaRetention` | `agent_logs` for LLM methods; `notifications` table for sent campaigns; `recommendGrowthPath()` remains deterministic with no event trigger of its own (pre-existing, out of this batch's named-finding scope) | Command Center agent activity table + Membership/Retention card | Confirmed |
| **TESS** | `daily_territory_analysis` cron + 8 growth-signal events → `handleTessTerritory` / `routeGrowthAutomationEvent()` | Cron-driven primarily; **now also drives NOVA's 3 expansion methods within the same handler invocation** (this batch's fix) | `agent_logs` + dedicated `audit_logs` row per run, now including a `nova_territories_analyzed` summary | Command Center agent activity table + Territory Readiness card | **Changed by this batch** |
| **GABRIEL** | Every automation event, unconditionally (`router.ts:272-282`) | Synchronous, inline in the router after every dispatch | `agent_logs` row with `action: "Governance Audit"` for every event processed | Command Center agent activity table | Confirmed for the core governance-audit function; `generateExecutiveBriefing()` removed as dead code (see `AGENT_TRIGGER_CERTIFICATION.md`) |

## Findings

1. **All 10 agents now have at least one verified, reachable Trigger → Execution → Evidence → Visibility chain**, with no method on any agent class left orphaned: every method is either invoked by a real event/cron/API route, or has been removed.
2. **Two evidence types exist on this platform and both count**: an `agent_logs` row (for LLM-backed calls) and a direct table write (`providers.trust_score`, `market_demand`, `revenue_records`, etc., for deterministic/computed paths). REX's deterministic trust-score path and NOVA's newly-wired market-intelligence writes both fall into the second category — absence of an `agent_logs` row does not mean absence of evidence.
3. **No documentation-only proof remains in this register**: every row above is sourced to a specific file (`router.ts` switch case, handler file, or API route), not to a prior certification's prose description.

**Status: CERTIFIED ✅** — every one of the 10 agents has a confirmed, traced execution path; the two gaps this batch found in the prior certification's accuracy (NOVA/MAX's true unreachability, and FINN/GABRIEL's true dead-code status) have both been closed by code changes documented in `AGENT_TRIGGER_CERTIFICATION.md`.
