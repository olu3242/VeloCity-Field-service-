import { AppShell } from "@/components/layout";

export default function FranchiseLayout({ children }: { children: React.ReactNode }) {
  return <AppShell variant="franchise">{children}</AppShell>;
}
